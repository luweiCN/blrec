# BLREC 3.0.0-beta.4

这是面向群晖灰度运行的公开测试版小修订。自动化测试和生产构建已经通过，但尚未完成真实环境验收。

## 修复

- 修复生产环境中打开录制中分批时，播放器可能长时间停留在“正在打开本地视频”的问题。
- 关闭在 Angular 分包环境中无法可靠启动的转封装 Web Worker，同时保留 Range 请求、有限文件快照和懒加载，避免下载完整录像。

升级仅替换镜像，不需要迁移设置；升级前仍应备份 `/cfg` 对应的宿主目录和部署 `.env`。
