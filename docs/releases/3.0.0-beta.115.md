# BLREC Server 3.0.0-beta.115

- Analysis Worker 在已识别对局内按 15 秒轻量检查头像布局，每局固定保留两个分散时段的清晰 HUD，并在出现时保留一张最清晰积分板。
- 商店、半透明和低置信困难画面继续作为少量困难样本保留，但不再挤掉正常地图、HUD 和积分板素材。
- 候选采样复用常驻英雄头像模型；一次模型加载后单帧推理保持毫秒级，不新增完整核心模型粗扫。
