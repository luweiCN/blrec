# BLREC 3.0.0-beta.3

这是面向群晖多网络灰度运行的公开测试版小修订。NAS 启动和双线路探测已经通过，但尚未完成真实环境验收，仍需继续观察 3～5 天。

## 修复

- 公网出口 IP 改由 B 站区域接口直接返回，避免第三方 IP 服务在部分宽带上不可达。
- 每块网卡的一次连通性检测由两个 HTTP 请求减少为一个，同时保留延迟、出口 IP 和可用性结果。

升级前请保留 `/volume1/docker/blrec-next/backups/` 中的 beta2 备份；升级仅替换镜像，不需要再次迁移旧版设置。
