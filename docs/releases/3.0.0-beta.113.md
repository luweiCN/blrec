# BLREC Server 3.0.0-beta.113

- 录制生命周期元数据改为先写入 NAS 本地持久化 journal，再异步幂等同步到 PostgreSQL；远程数据库暂时不可用时，录像文件与本地录制事实仍可继续落盘。
- PostgreSQL 恢复后会先补齐本地积压，再恢复上传与分析任务；新增本地 journal 健康状态与升级前一致性备份。
- B 站以“视频内部存在时间戳跳变”拒绝稿件时，自动重封装视频并仅重试一次；已有失败任务也可通过原有重试入口重新处理。
