# BLREC 3.0.0-beta.126

- 保留历史录播 16 路下载能力，将 yt-dlp 高频进度改为内存最新值覆盖、每 5 秒限频持久化并在结束时强制刷新，避免慢数据库回调阻塞 stdout、让已完成传输的任务继续占用下载槽。
- 群晖发布附件新增 PVE PostgreSQL LAN 直连 overlay 和无凭据环境示例；生产默认配置固定为 `192.168.50.62:5432/blrec_dashboard`，旧公网 SSH tunnel 只保留为显式回滚路径。
