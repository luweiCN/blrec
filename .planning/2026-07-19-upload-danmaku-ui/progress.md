# 调查进度

## 2026-07-19

- 已记录本轮完整需求与成功标准。
- 已读取系统化诊断、需求设计和持久化研究流程。
- 已完成暂停任务第一轮数据库取证，并确认五条暂停任务的不同原因。
- 下一步：获取 Java 项目实际版本与 NAS 新旧服务信息，建立代码和现场数据差异对照。
- 已确认 NAS 的 Java 镜像、创建日期和对应源码提交，并确认旧 BLREC 的录像/日志挂载路径。
- 已完成 Java/Python 预上传参数、请求头和重试边界的第一轮逐行对照。
- 已完成创建稿件载荷、buvid 补充、验证码处理及后台自动重试差异对照。
- 已安全统计 Java 容器过去一周的实际重试/失败日志：6 次投稿响应，未触发上述错误路径。
- 已还原任务 17/18 的精确预上传时间线，基本定位 406 为连续创建 10 个 UPOS 会话后的短时限流。
- 已克隆 API 备份仓库，并对照了当前预上传 profile、服务端重试字段、稿件必要字段和 buvid 风控说明。
- 已搜索公开 issue 和当前创作中心入口资源，暂未找到 21588 的公开错误码解释。
- 已确认上传页逻辑不在创作中心静态 HTML 内，需从动态路由资源继续追踪。
- 已解析 Webpack 路由和资源命名规则，定位当前普通视频上传 chunk。
- 已继续解析 chunk，定位真正的视频投稿微应用入口 `/york/videoup?new`。
- 已从 B 站当前上传前端源码明确定位 21588 为按 CID 返回的 BVC 视频检测失败，而非投稿文本字段错误。
- 已从同一版本前端确认 HTTP 406 的用户语义就是“上传视频过于频繁，请稍后再试”，并确认官方上传器启用了自动重试。
- 已对任务 21 四个实际录像做 ffprobe；发现 P2 只有 0.901 秒，违反 B 站官方“时长至少 1 秒”的前置校验。
- 已确认 Java 版默认过滤/删除小于 60 秒的异常碎片，这能解释其历史上很少遇到同类 BVC 拒绝。
- 已还原高光任务 2、3 的历史创建链路：它们来自 7 月 17 日上午的旧交互，旧按钮会先建人工暂停任务再编辑；当天中午已改为保存投稿弹窗后才建可运行任务，beta.15 已包含。
- 已完成新旧服务同场 XML/JSONL 对照，确认新匿名弹幕 WebSocket 收到的消息仅约旧登录态连接的 1/6～1/10；连接本身全程健康，问题是匿名消息流不完整而非断线或统计错误。
- 已确认上传任务 API 已聚合弹幕总数、成功、待发送、未知和失败计数；当前缺口只是列表未呈现，详情抽屉已有基础进度。
- 已完成全局页面壳与表格宽度初查：所有页面被统一限制为 1180px，但数据页仍大量使用固定列宽和横向滚动；后续应建立明确的“数据页/编辑页”两种统一宽度规格，而不是逐页写宽度补丁。
- 已确认当前高光文件仍位于 `/rec/highlights`，且一旦生成高光上传场次就会被录制容量统计纳入；必须拆出独立 clip root，并在容量查询中显式排除 `source_kind='highlight'`。
- 已实现动态预上传窗口：从 1 开始、成功递增至 5，406/429 收缩并递增冷却，UPOS 分块和完成阶段的瞬时错误自动恢复。
- 已实现不足 60 秒录像分片只排除投稿、保留本地文件，并在录像详情记录排除原因。
- 已保存 UPOS CID 与 21588 的安全 BVC 详情，可将错误映射到本地分 P。
- 已将投稿网络未知、5xx、频控和进程重启改为退避后自动续跑；明确内容拒绝、账号、配置和本地文件错误仍停止处理。
- 已将弹幕 WebSocket 改为登录态优先、匿名兜底，并把视频播放与分页弹幕列表合并。
- 已新增 `/clips` 永久片段目录、旧片段迁移、容量排除和全局片段管理页面；片段投稿只在确认完整投稿设置后创建任务。
- 已重排房间、录制和上传任务列表，以复合单元格展示投稿、录像、弹幕和处理进度，桌面布局不依赖横向滚动。
- 审查后补充使用 ffprobe 真实时长过滤、终场全短分片自动收敛、UPOS 完成阶段退避、人工暂停隔离及旧片段路径身份迁移。
- 投稿结果未知时按标题与 UPOS 远端文件名持续核对近期稿件，不再自动重发非幂等投稿请求；明确未发出的请求仍自动续跑。
- 弹幕分页改为服务端流式游标与前端最多 1000 条的滑动窗口；手动翻页和播放位置同步互不覆盖。房间列表在窄桌面宽度提前切换为卡片布局。
- ffprobe 连续 5 次失败后自动隔离损坏分 P，不再阻塞同场正常录像；删除片段会等待上传 worker 确认收到停止信号后再清理本地状态。
- 历史失败任务不再通过通用数据库迁移清空，避免误伤本地文件已经删除、只剩远端分 P 的任务；NAS 上指定的 17、18、21 会在部署前备份并核对本地文件后定向重建。弹幕认证失败按发起请求或连接时的 Cookie 指纹精确归因；旧凭据回调不会误停新凭据或备用账号，自动续期后的重试仍失败才暂停并匿名兜底。
- 已通过 850 项后端测试、344 项前端测试、24 项浏览器插件测试、mypy、Black、isort、Flake8、TypeScript 类型检查及 Angular/插件生产构建；等待发布工作流和 NAS 实机验证。
