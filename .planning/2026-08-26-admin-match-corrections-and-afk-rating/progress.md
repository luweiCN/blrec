# Progress

- 2026-08-26：建立独立工作计划；先处理计分反馈回路和部署拓扑，再进入内网站长纠错功能。
- 2026-08-26：采用既有产品视觉系统，内网站长端以高密度、可追溯、明确保存状态为设计方向，不新增装饰性视觉语言。
- 2026-08-26：复现双方同数挂机仍扣分的代码路径；确认现有测试把这一行为写成了 `none`，需要先翻转为失败测试。
- 2026-08-26：确认公网“站长入口”目前只是 token 控制的只读 owner 视图；Vision Lab 已有 `manual_correction` 导入能力，可作为纠错训练回流目标。
- 2026-08-26：确认 Dashboard API workflow 仍使用旧 SSH 目标 secrets，临时 hostname guard 只负责止血，后续将以 PVE 正式发布路径整体替换。
- 2026-08-26：先写双方同挂、对方挂机更多两种输局保护回归，确认旧实现失败后将保护条件改为“己方非本人队友至少一人挂机”；67 个评分测试全部通过。胜局保留现有规则：实际少打多才奖励，双方同人数按正常胜局计分，不因对方挂机倒扣或打折。
- 2026-08-26：用 PVE self-hosted runner 的 build/deploy 两段 workflow 替换旧 SSH 目标发布；删除数据库 SSH 隧道 unit 和安装逻辑，API 改为直接连接 PVE PostgreSQL。API 测试 68 通过、4 个需外部 PostgreSQL 的用例跳过。
- 2026-08-26：恢复未完成的内网站长编辑器；已去掉新增 `@angular/forms` 依赖的方案，改用原生表单事件，下一步同时验证公开构建不暴露编辑能力、内网构建可调用可信反代 API。
- 2026-08-26：公开 Dashboard 和 PVE 内网站长构建均通过；公开环境关闭管理能力，内网环境才启用 `/internal-api` 编辑入口。
- 2026-08-26：增加受信 PVE 反代密钥与现有管理员 session 并存的认证测试，先复现 401，再实现常量时间密钥校验；错误密钥仍为 401。BLREC 对应 49 个路由测试通过。
- 2026-08-26：新增 PVE 内网站长不可变静态 release、root-only Nginx 密钥注入、同源限制、健康检查和原子回滚；公开 OSS/CDN 发布仍保留。删除 API 数据库隧道示例并改正 PVE 直连文档。
- 2026-08-26：英雄识别概率迁移先写失败测试，再增加 schema v83；新分析、整场重扫、单局重跑和英雄补扫均保存模型原概率，历史空值明确展示“历史未保存”。数据库/仓库/Web 相关 98 个测试通过。
- 2026-08-26：内网站长一次 PATCH 同时提交对局字段、玩家字段、主播位置和挂机人工覆盖，避免先保存一半、第二次请求失败的部分成功状态。
